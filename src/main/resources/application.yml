spring:
  application:
    name: kotlin-mysql
  datasource:
    url: jdbc:mysql://db:3306/mysql
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080